# 에이전트 작업 지침

이 파일은 코드 작업을 하는 AI 에이전트가 반드시 읽고 따라야 할 지침입니다.
**코드를 수정하거나 새 기능을 추가하기 전에 이 파일을 먼저 읽으세요.**

---

## 필수: 문서 동기화

코드를 변경할 때는 **관련 문서도 함께 업데이트**해야 합니다.
문서는 `docs/` 폴더에 주제별로 분리되어 있습니다.

### 어떤 문서를 업데이트할지 판단 기준

| 변경 내용 | 업데이트할 문서 |
|-----------|----------------|
| 새 페이지/컴포넌트 추가 | `08-frontend.md`, `01-overview.md` (디렉토리 구조) |
| 새 API 엔드포인트 추가 | `04-api.md` |
| 새 Socket.io 이벤트 추가 | `05-websocket.md` |
| DB 스키마 변경 (prisma) | `06-database.md` |
| 인증 로직 변경 | `03-auth.md` |
| 게임 모드 추가/변경 | `07-game-modes.md` |
| 서버 파일 구조 변경 | `09-backend.md`, `01-overview.md` |
| 라우팅 변경 (App.jsx) | `08-frontend.md` |
| 아키텍처 수준의 변경 | `02-architecture.md` |
| 환경변수 추가 | `01-overview.md`, `README.md` |

### 업데이트 원칙

- **추가**한 기능은 해당 문서에 추가합니다.
- **삭제**한 기능은 해당 문서에서도 제거합니다.
- **변경**한 시그니처(함수 파라미터, 이벤트 페이로드, API 스펙)는 문서에서도 동일하게 수정합니다.
- 문서가 코드와 어긋나면 **코드 기준**으로 수정합니다.

---

## 프로젝트 이해

작업 전에 관련 문서를 읽어 컨텍스트를 파악하세요.

```
docs/
├── 01-overview.md      ← 전체 구조, 기술 스택
├── 02-architecture.md  ← 시스템 아키텍처
├── 03-auth.md          ← 인증 흐름, 게스트 모드
├── 04-api.md           ← REST API 명세
├── 05-websocket.md     ← Socket.io 이벤트
├── 06-database.md      ← DB 스키마, 쿼리
├── 07-game-modes.md    ← 게임 모드별 흐름
├── 08-frontend.md      ← 훅, 컴포넌트, 페이지
└── 09-backend.md       ← 서버 파일, 소켓 핸들러
```

---

## 핵심 설계 원칙 (변경 시 주의)

다음은 의도적인 설계 결정입니다. 변경 전에 이유를 이해하고 판단하세요.

### useRef로 게임 상태 관리
`useChessGame`은 chess.js 인스턴스를 `useRef`로 관리합니다.
Socket.io 이벤트 리스너 내부에서 stale closure 없이 최신 게임 상태를 참조하기 위함입니다.
`useState`로 바꾸면 온라인 게임에서 이동이 씹히는 버그가 생깁니다.

### 인메모리 게임 룸
온라인/매칭 게임의 룸 상태는 DB가 아닌 `gameManager.js`의 `Map`에 저장합니다.
실시간 체스의 매 이동마다 DB 접근은 불필요하기 때문입니다.
게임 종료 시에만 최종 결과를 DB에 저장합니다.

### 이중 이동 검증
클라이언트(chess.js) + 서버(gameManager.applyMove) 양쪽에서 이동을 검증합니다.
서버 검증을 제거하면 임의 이동 조작이 가능해집니다.

### SuperSchool 프록시
클라이언트가 SuperSchool API를 직접 호출하지 않습니다.
CORS 방지 + 학교 인증 자격증명의 브라우저 노출 방지를 위한 결정입니다.

### 게스트 모드 이메일 처리
게스트는 이메일이 없어 game_results에 저장되지 않습니다.
서버의 저장 로직은 email 유무를 확인하고 null이면 스킵합니다.
이 동작을 변경하면 게스트의 게임이 DB에 쌓이거나 오류가 발생합니다.

---

## 체크리스트

코드 변경을 완료하기 전에 확인하세요:

- [ ] 관련 `docs/*.md` 파일을 업데이트했는가?
- [ ] 새 이벤트/엔드포인트라면 `04-api.md` 또는 `05-websocket.md`에 추가했는가?
- [ ] 새 파일을 만들었다면 `01-overview.md`의 디렉토리 구조에 추가했는가?
- [ ] `README.md`에 영향을 주는 변경(실행 방법, 환경변수 등)이라면 업데이트했는가?
